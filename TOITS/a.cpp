#include <bits/stdc++.h>
using namespace std;

int main(int argc, char *argv[])
{
  int n, q, i;
  cin >> n >> q;
  vector<pair<int, int>> data(n);
  i = 0;
  while (i < q)
  {
    cin >> i >> data[]
  }
  return 0;
}
