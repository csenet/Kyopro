#include <bits/stdc++.h>
using namespace std;

int l;
main(void)
{
  cin >> l;
  long double w = l / 3.0;
  std::cout << std::fixed << std::setprecision(10);
  cout << w * w * w << endl;
  return 0;
}